\MPragma{MathSkip}

%LaTeX-2e-File, Liedtke, 20140731.
%Inhalt: Einf"uhrung in die Differentialrechnung, Abschnitt 5.
%zuletzt bearbeitet: 20140922.

%Abschnitt 5: Anwendungen der Differentialrechnung
%\MSubsubsection{Anwendungen}
%\begin{MContent}


\begin{MXContent}{Kurvendiskussion}{Kurvendiskussion}{STD}

Gegeben ist eine differenzierbare Funktion $f: (a, b) \rightarrow \R$ 
mit Zuordnungsvorschrift $y = f(x)$ f"ur $x \in (a, b)$.
Eine vollständige Kurvendiskussion für $f$ besteht in diesem Kurs aus folgenden Angaben:

\begin{enumerate}
\item Maximaler Definitionsbereich
\item Achsenschnittpunkte des Graphen
\item Symmetrie des Graphen
\item Grenzverhalten/Asymptoten
\item Die ersten Ableitungen
\item Extremstellen
\item Monotonieverhalten
\item Wendestellen
\item Krümmungsverhalten
\item Skizze des Graphen
\end{enumerate}

Viele dieser Punkte wurden bereits in Modul \MRef{VBKM06} besprochen. Daher
werden wir im Folgenden nur kurz wiederholen, was wir unter den einzelnen
Schritten der Kurvendiskussion verstehen wollen. Im Anschluss werden wir
uns eine Kurvendiskussion detailliert an einem Beispiel ansehen.

Der erste Teil der Kurvendiskussion besteht aus algebraischen und geometrischen Aspekten von $f$:

\begin{description}
\item[Maximaler Definitionsbereich]
Wir bestimmen alle reellen Zahlen $x$, f"ur die $f(x)$ existiert. Die 
Menge $D$ all dieser Zahlen wird maximaler Definitionsbereich genannt.

\item[Symmetrie des Graphen]
Der Graph der Funktion ist symmetrisch zur $y$-Achse, wenn $f(-x) = f(x)$ f"ur
alle $x \in D$ ist. Ist $f(-x) = -f(x)$ f"ur alle $x \in D$, dann ist der Graph
zum Ursprung $(0;0)$ des Koordinatensystems punktsymmetrisch.

\item[Schnittpunkte mit den Achsen] ~\relax
\begin{itemize}
\item $x$-Achse: Wir bestimmen alle Nullstellen von $f$.
\item $y$-Achse: Wir berechnen den Funktionswert $f(0)$ (falls $0 \in D$).
\end{itemize}
\item[Asymptotisches Verhalten an den R"andern des Definitionsbereichs]
Wir untersuchen, die Grenzwerte der Funktion $f$ an den Grenzen ihres Definitionsbereichs.
\end{description} 

Im zweiten Teil wird die Funktion mittels Folgerungen aus der Ableitung 
analytisch untersucht. Dazu müssen wir natürlich zunächst die erste und zweite Ableitung 
berechnen, sofern diese existieren.

\begin{description}
\item[Ableitungen]
Berechnung der ersten und zweiten Ableitung (soweit vorhanden).

\item[Extemstellen und Monotonie]
Notwendige Bedingung f"ur Extremstellen: $f'(x) = 0$\newline
Wir berechnen also diejenigen Stellen $x_0$, an denen die Ableitung $f'$ den Wert Null annimmt. Wenn an diesen Stellen auch die zweite Ableitung $f''$ existiert, gilt:
\begin{itemize}
\item $f''(x_0) > 0$: $x_0$ ist eine Minimalstelle von $f$.
\item $f''(x_0) < 0$: $x_0$ ist eine Maximalstelle von $f$.
\end{itemize}
Die Funktion $f$ ist auf den Intervallen des Defitionsbereichs monoton
wachsend, wo $f'(x) \geq 0$ gilt. Sie ist monoton fallend, wenn $f'(x) \leq 0$ 
gilt.

\item[Wendestellen und Kr"ummungseigenschaften]
Notwendige Bedingung f"ur Wendestellen (wenn die zweite Ableitung $f''$ existiert): $f''(w) = 0$\newline
Wenn $f''(w_0) = 0$ und $f^{(3)}(w_0) \neq 0$ ist, dann ist $w_0$ eine Wendestelle, d.h. $f$ ändert an dieser Stelle das Krümmungsverhalten.\newline
Die Funktion $f$ ist auf den Intervallen des Defitionsbereichs konvex
(linksgekr"ummt), wo $f^{(2)}(x) \geq 0$ gilt. Sie ist konkav (rechtsgekr"ummt), 
wenn $f^{(2)}(x) \leq 0$ gilt.

\item[Skizze des Graphen] Wir fertigen eine Skizze des Graphen in einem geeigneten Koordinatensystem unter Berücksichtigung der während der Kurvendiskussion gewonnenen Daten.
\end{description}

\MSubsubsection{Ausführliches Beispiel}
Wir untersuchen die Funktion
$$
f(x) \;=\; \frac{4x}{x^2+2}\: .
$$

\textbf{Definitionsbereich}\\
Der maximale Definitionsbereich dieser Funktion ist $D_f=\R$, da der Nenner der Funktion $x^2+2\geq 2$ niemals Null wird und daher keine Stellen ausgeschlossen werden müssen.
\ \\ \ \\
\textbf{Achsenschnittpunkte}\\
Die Nullstellen der Funktion entsprechen den Nullstellen des Zählers. Daher schneidet $f$ die $x$-Achse nur im Nullpunkt $(0,0)$, denn der Zähler wird nur für $x=0$ zu Null. Dies ist auch der einzige Schnittpunkt mit der $y$-Achse, da $f(0)=0$ ist.
\ \\ \ \\
\textbf{Symmetrie}\\
Um das Symmetrieverhalten zu untersuchen, ersetzen wir $x$ durch $(-x)$. Es gilt
$$
f(-x) \;=\;\frac{4\cdot (-x)}{(-x)^2+2} \;=\; -\frac{4x}{x^2+2} \;=\; -f(x)
$$
für alle $x\in\R$. Der Graph von $f$ ist folglich punktsymmetrisch zum Ursprung.
\ \\ \ \\
\textbf{Grenzverhalten}\\
Die Funktion ist auf ganz $\R$ definiert, daher ist nur das Grenzverhalten für $x\rightarrow \infty$ und $x\rightarrow -\infty$ zu untersuchen.
Da $f(x)$ ein Bruch aus zwei Polynomen ist und der Nenner die höhere Potenz besitzt, ist die $x$-Achse die waagerechte Asymptote in beide Richtungen:
$f(x)\rightarrow 0$ für $x\rightarrow\infty$ sowie für $x\rightarrow -\infty$.
\ \\ \ \\
\textbf{Ableitungen}\\
Die ersten beiden Ableitungen der Funktion erhalten wir mit der Quotientenregel:
$$
f'(x) \;=\; 4\cdot\frac{1\cdot (x^2+2)-x\cdot 2x}{(x^2+2)^2} \;=\; 4\cdot \frac{-x^2+2}{(x^2+2)^2}\: .
$$
Erneutes Ableiten und Vereinfachen ergibt
\begin{eqnarray*}
f''(x) & = & 4 \cdot % 
\frac{-2x (x^2 + 2)^2 - (-x^2 + 2) \cdot 2 (x^2 + 2) \cdot 2 x}{(x^2 + 2)^4} \\
& = & 4 \cdot \frac{-2x (x^2 + 2) - (-x^2 + 2) \cdot 4 x}{(x^2 + 2)^3} \\
& = & 4 \cdot \frac{-2x^3 - 4x + 4x^3 - 8 x}{(x^2 + 2)^3} \\
& = & 4 \cdot \frac{2x^3 - 12x}{(x^2 + 2)^3} \\
& = & 8 \cdot \frac{x (x^2 - 6)}{(x^2 + 2)^3}\: .
\end{eqnarray*}
\ \\
\textbf{Extremstellen}\\
Die notwendige Bedingung für eine Extremstelle, $f'(x)=0$, ist hier gleichbedeutend mit $-x^2+2=0$. Wir erhalten also $x_1=\sqrt2$ und $x_2=-\sqrt2$. Untersuchen wir an diesen Stellen auch das Verhalten der zweiten Ableitung:
$$
f''(x_1) \;=\; 8\frac{\sqrt2\cdot(2-6)}{(2+2)^3}<0\;\; , \;\;
f''(x_2) \;=\; 8\frac{-\sqrt2\cdot(2-6)}{(2+2)^3}>0\;\; .
$$
Folglich ist $x_1$ eine Maximalstelle und $x_2$ eine Minimalstelle von $f$. Durch Einsetzen in $f$ erhalten wir den Hochpunkt $(\sqrt2,\sqrt2)$ und den Tiefpunkt $(-\sqrt2,-\sqrt2)$ von $f$.
\ \\ \ \\
\textbf{Monotonieverhalten}\\
Da $f$ auf ganz $\R$ definiert ist, kann das Monotonieverhalten an den Extremstellen abgelesen werden: $f$ ist monoton fallend auf $(-\infty,-\sqrt2)$, monoton wachsend auf $(-\sqrt2,\sqrt2)$
und monoton fallend auf $(\sqrt2,\infty)$. Monotonieintervalle geben wir stets in offener Form an.
\ \\ \ \\
\textbf{Wendestellen}\\
Aus der notwendigen Bedingung für Wendestellen $f''(x)=0$ erhalten wir die Gleichung $8x(x^2-6)=0$. Somit sind $w_0=0$, $w_1=\sqrt6$ und $w_2=-\sqrt6$ die einzigen Lösungen.
Das Polynom im Nenner von $f''$ ist stets größer als Null. Da das Zählerpolynom nur einfache Nullstellen besitzt, ändert $f''(x)$ in allen diesen Stellen das Vorzeichen. Es handelt sich
daher um Wendestellen von $f$. Die Wendepunkte $(0,0)$, $(\sqrt6,\frac12\sqrt6)$, $(-\sqrt6,-\frac12\sqrt6)$ des Graphen von $f$ ergeben sich durch Einsetzen in $f$.
\ \\ \ \\
\textbf{Krümmungsverhalten}\\
Die zweimal differenzierbare Funktion $f(x)$ ist konvex, wenn die zweite Ableitung größer oder gleich Null ist. Sie ist konkav, wenn die zweite Ableitung kleiner oder gleich Null ist.
Da das Polynom im Nenner von $f''$ stets positiv ist, genügt es, das Vorzeichen des Polynoms $p(x)=8x(x-\sqrt6)(x+\sqrt6)$ im Zähler zu untersuchen. Für $0<x<\sqrt6$ ist es negativ (dort ist $f$ konkav). Für $x>\sqrt6$ ist es positiv (dort ist $f$ konvex). Da $f$ punktsymmetrisch ist folgt, dass $f$ auf den Intervallen $(-\sqrt 6,0)$ und $(\sqrt6,\infty)$ konvex sowie auf $(-\infty,-\sqrt6)$ und $(0,\sqrt6)$ konkav ist.
\ \\ \ \\
\textbf{Skizze des Graphen}\\
\MUGraphics{\MPfadBilder/BildKurvendiskussion1.png}{width=0.5\linewidth}{Die Funktion $f(x)$ skizziert auf dem Intervall $[-8,8]$.}{width:600px}

%\textbf{2. Funktion mit Logarithmusterm:}\\
%Wir untersuchen die Funktion
%$$
%f(x) \;=\; \frac{\ln^2 x}{x}\: .
%$$

%\textbf{Definitionsbereich}\\
%Sehen wir uns die gegebene Funktion $f(x) = \frac{(\ln x)^2}{x}$ näher an, dann stellen wir zunächst fest, dass der Nenner nicht zu Null werden darf. Außerdem ist der natürliche Logarithmus nur für %positive $x$ definiert. Damit ergibt sich der maximale Definitionsbereich $D_f = (0, \infty)$.
%\ \\ \ \\
%\textbf{Achsenschnittpunkte}\\
%Auch hier werden die Nullstellen durch die Zählerfunktion bestimmt. Die einzige Nullstelle (Schnittpunkt mit $x$-Achse) liegt bei $x=1$, also im Punkt $(1,0)$ des Graphen, da die Nullstelle des natürlichen Logarithmus bei $x = 1$ liegt. Einen Schnittpunkt mit der $y$-Achse gibt es nicht, da $x=0$ nicht zum Definitionsbereich der Funktion gehört.
%\ \\ \ \\
%\textbf{Symmetrie}\\
%Da der Definitionsbereich die Voraussetzungen nicht 
%erf"ullt, er"ubrigt sich die Frage nach der Symmetrie.
%\ \\ \ \\
%\textbf{Grenzverhalten}\\
%Die $y$-Achse $x = 0$ ist eine vertikale Asymptote für $x\rightarrow 0$ (von rechts) und wegen
%$$
%\lim_{x \rightarrow \infty} \frac{\ln^2 x}{x} %
%\; = \;\lim_{x \rightarrow \infty} \frac{\frac{2 \ln x}{x}}{1} %
%\; = \;\lim_{x \rightarrow \infty} \frac{2 \ln x}{x} %
% \;= \;\lim_{x \rightarrow \infty} \frac{2 \frac{1}{x}}{1} %
% \;= \;\lim_{x \rightarrow \infty} \frac{2}{x} %
% \;=\; 0 %%
%$$
%ist die $x$-Achse eine Asymptote f"ur $x \rightarrow \infty$.
%\ \\ \ \\
%\textbf{Ableitungen}\\
%Für $x>0$ erhalten wir
%\begin{eqnarray*}
%f'(x) & = & \frac{\frac{2 \ln x}{x} x - (\ln^2 x) \cdot 1}{x^2} %
% = \frac{2 \ln x - \ln^2 x}{x^2} %
% = \frac{(2 - \ln x) \ln x}{x^2}\\
%f''(x) & = & \frac{(\frac{2}{x} - \frac{2 \ln x}{x}) \cdot x^2 %
% - (2 \ln x - \ln^2 x) \cdot 2 x}{x^4} %
% = 2 \cdot \frac{1 - 3 \ln x + \ln^2 x}{x^3} \\
%& = & 2 \cdot \frac{(\ln x)^2 - 3 \ln x + 1}{x^3}\: .
%\end{eqnarray*}
%\ \\
%\textbf{Extremstellen und Monotonieverhalten}\\
%Nullstellen von $f'$ sind $x_1 = 1$ und $x_2 = \MEU^2$.
%Weiter ist $x_1 < x_2$ und 
%$$
%f'(x)\; =\; - \frac{\ln x (\ln x - 2)}{x^2}\: .
%$$
%Auf $(0,1)$ ist $f'$ negativ und damit $f$ monoton fallend.
%Andererseits ist $f'$ auf $(1, \MEU^2)$ positiv und damit $f$ monoton wachsend.
%Auf $(\MEU^2, \infty)$ ist $f'$ negativ und damit $f$ monoton fallend.
%Aus dieser Argumentation können wir ohne Untersuchung der zweiten Ableitung folgern, dass $x_1 = 1$ Minimalstelle ist und $x_2 = \MEU^2$ eine Maximalstelle ist.
%Damit ist $(1, 0)$ Tiefpunkt und $(\MEU^2, \frac{4}{\MEU^2})$ Hochpunkt des Graphen von $f$.
%\ \\ \ \\
%\textbf{Wendestellen}\\
%Die notwendige Bedingung f"ur Wendestellen $f''(x) = 0$ f"uhrt auf die Gleichung $(\ln x)^2 - 3 \ln x + 1 = 0$.
%Mit der Substitution $z := \ln x$ ergibt sich die Gleichung $z^2 - 3 z + 1 = 0$, deren L"osungen
%\begin{eqnarray*}
%z_1 \;= \;\frac{3 - \sqrt{9 - 4}}{2} \;= \;\frac{3 - \sqrt{5}}{2}, \\
%z_2 \;= \;\frac{3 + \sqrt{9 - 4}}{2} \;=\; \frac{3 + \sqrt{5}}{2} %%
%\end{eqnarray*}
%sind. Aus $\ln x = z_1$ folgt die L"osung $w_1 := \MEU^{z_1}$ und aus $\ln x = z_2$ ergibt sich die zweite L"osung $w_2 := \MEU^{z_2}$ der Gleichung $f''(x)=0$. Damit ist
%$$
%f''(x) \;= \;2 \cdot \frac{(\ln x)^2 - 3 \ln x + 1}{x^3} \;=\; 2 \cdot \frac{(x - w_1) \cdot (x - w_2)}{x^3}\: .
%$$
%Folglich "andert sich das Vorzeichen von ${f'}'$ in $w_1$ bzw. 
%in $w_2$. Daher sind $w_1$ und $w_2$ Wendestellen von $f$.
%Die Wendepunkte lauten damit $(w_1, f(w_1))$ und $(w_2, f(w_2))$.
%Aus $0 < w_1 < w_2$ und der Form der zweiten Ableitung ergibt sich, dass
%${f'}'$ auf $(0, w_1)$ positiv und damit $f$ konvex, ${f'}'$ auf $(w_1, w_2)$ negativ und damit $f$ konkav,
%sowie ${f'}'$ auf $(w_2, \infty)$ positiv und damit $f$ konvex ist.
%\ \\ \ \\
%\textbf{Skizze des Graphen}\\
%\MUGraphics{\MPfadBilder/BildKurvendiskussion2.png}{width=0.5\linewidth}{Die Funktion $f(x)$ skizziert auf dem Intervall $(0,16)$.}{width:600px}
\end{MXContent}

\begin{MExercises}

\begin{MExercise}
Führen Sie eine vollständige Kurvendiskussion für die Funktion $f(x) = (2 x - x^2) \MEU^x$ und füllen Sie die Eingabefelder mit Ihren Ergebnissen aus:
\ \\ \ \\
Definitionsbereich: \MIntervalQuestion{25}{(-infty,infty)}{5} \MInputHint{(in Intervallschreibweise)}.\\
\ \\
Menge der Schnittpunkte mit der $x$-Achse (Nullstellen von $f(x)$): \MParsedQuestion{5}{0,2}{5} \MInputHint{(in Mengenschreibweise, nur $x$-Komponente)}.\\
\ \\
Einziger Schnittpunkt mit der $y$-Achse ist bei \MIntervalQuestion{25}{(0,0)}{5} \MInputHint{(in Punktschreibweise)}.\\
\ \\
\begin{MQuestionGroup}
Symmetrie: \MCheckbox{0} $y$-Achsensymmtrisch\ \ \ \MCheckbox{0} Punktsymmetrisch zum Ursprung
\end{MQuestionGroup}
\MGroupButton{Auswahl überprüfen}
\ \\
Grenzverhalten: Die Asymptoten am Rand sind \MSimplifyQuestion{20}{-infty}{4}{infty}{4}{1} am für $x\rightarrow \infty$ und \MParsedQuestion{15}{0}{3} für $x\rightarrow-\infty$.\\
\ \\
Ableitungen: Es ist $f'(x)$ = \MSimplifyQuestion{20}{-(x^2-2)*exp(x)}{4}{x}{4}{0} sowie ${f'}'(x)$ = \MSimplifyQuestion{30}{-(x^2+2*x-2)*exp(x)}{5}{x}{5}{0} .\\
\ \\
Monotonieverhalten: Die Funktion ist auf dem Intervall \MIntervalQuestion{26}{(-sqrt(2),sqrt(2))}{4} monoton wachsend und ansonsten monoton fallend.\\
\ \\
Extremwerte: Die Stelle $x_1$ = \MParsedQuestion{13}{-sqrt(2)}{3} ist ein Minimum und $x_2$ = \MParsedQuestion{13}{sqrt(2)}{3} ist ein Maximum.\\
\ \\
Wendepunkte: Die Menge der Wendepunkte (nur $x$-Komponente) ist \MParsedQuestion{30}{-1-sqrt(3),-1+sqrt(3)}{3}\\\MInputHint{(in Mengenschreibweise, Wurzeln dürfen stehenbleiben)}.\\
\ \\
Skizzieren Sie den Graph und vergleichen Sie ihn anschließend mit der Musterlösung:

\begin{MSolution}
\textbf{Definitionsbereich}\\
Es gilt $f(x) = - x (x - 2) \MEU^x$ und $\MEU^x > 0$ f"ur alle  $x \in \R$, damit ist $D_f = \R$ der maximale Definitionsbereich.
\ \\ \ \\
\textbf{Achsenschnittpunkte}\\
Schnittpunkte mit der $x$-Achse (Nullstellen der Funktion) liegen bei $x=0$ und $x=2$, was auf die Punkte $(0,0)$ und $(2,0)$ führt. Der Schnittpunkt mit der $y$-Achse ist $(0,0)$.
\ \\ \ \\
\textbf{Symmetrie}\\
Die Funktion $f$ ist weder gerade noch ungerade und damit der Graph von $f$ weder $y$-achsensymmetrisch noch punktsymmetrisch bzgl. des Ursprungs.
\ \\ \ \\
\textbf{Grenzverhalten}\\
Da die Funktion für alle reellen Zahlen definiert ist, müssen nur die Asymptoten bei $\pm \infty$ untersucht werden:
$$
\lim_{x \rightarrow \infty} - x (x - 2) \MEU^x \; =\; - \infty \;\text{und}\;
\lim_{x \rightarrow -\infty} - x (x - 2) \MEU^x \;=\;  0 \: .
$$
Somit ist $y = 0$ f"ur $x \rightarrow -\infty$ eine Asymptote.
\ \\ \ \\
\textbf{Ableitungen}\\
Die ersten beiden Ableitungen von $f(x)$ sind
\begin{eqnarray*}
f'(x) & = & (2 - 2 x) \MEU^x + (2 x - x^2) \MEU^x %
 = (2 - x^2) \MEU^x = - (x^2 - 2) \MEU^x \: ,\\
{f'}'(x) & = & - 2 x \MEU^x + (2 - x^2) \MEU^x %
 = - (x^2 + 2 x - 2) \MEU^x \: .
\end{eqnarray*}
\ \\
\textbf{Monotonieverhalten und Extremwerte}\\
L"osungen von $f'(x) = 0$ sind $x_1 = -\sqrt{2}$ und $x_2 = \sqrt{2}$. 
Weiter ist $x_1 < x_2$ und 
$$
f'(x)\; =\; - (x + \sqrt{2}) (x - \sqrt{2}) \MEU^x\: .
$$
Auf $(-\infty, -\sqrt{2})$ ist $f'$ negativ und damit $f$ monoton fallend.
Auf $(-\sqrt{2}, \sqrt{2})$ ist $f'$ positiv und damit $f$ monoton wachsend.
Auf $(\sqrt{2}, \infty)$ ist $f'$ negativ und damit $f$ monoton fallend.
Somit ist $x_1 = -\sqrt{2}$ Minimalstelle und $x_2 = \sqrt{2}$ Maximalstelle.
\ \\ \ \\
\textbf{Wendepunkte}\\
Die notwendige Bedingung f"ur Wendepunkte ${f'}'(x) = 0$ f"uhrt auf die quadratische Gleichung
$x^2 + 2 x - 2 = 0$. Die L"osungen sind $w_1 = \frac{-2 - \sqrt{4 + 8}}{2} = -1 - \sqrt{3}$
und $w_2 = \frac{-2 + \sqrt{4 + 8}}{2} = -1 + \sqrt{3}$.
\ \\ \ \\
\textbf{Skizze des Graphen}\\
\MUGraphics{\MPfadBilder/BildKurvendiskussion3.png}{width=0.5\linewidth}{Die Funktion $f(x)$ skizziert auf dem Intervall $(-6,2.3)$.}{width:400px}
\end{MSolution}

\end{MExercise}

\end{MExercises}



%\MSubsubsection{Optimierungsaufgaben}
\begin{MXContent}{Optimierungsaufgaben}{Optimierungsaufgaben}{STD}

In vielen Anwendungen der Technik oder Wirtschaft findet man Problemlösungen, die nicht eindeutig sind. Häufig hängen sie von variablen Bedingungen ab. Um eine ideale Lösung zu finden, werden zusätzliche Eigenschaften (Nebenbedingungen) definiert, die von der Lösung erfüllt werden müssen. Dies führt sehr oft zu sogenannten \textbf{Optimierungsaufgaben}, bei denen aus einer Schar von Lösungen diejenige gesucht werden muss, die eine vorab festgelegte Eigenschaft am besten erf"ullt.

Als Beispiel betrachten wir die Aufgabe, eine zylinderf"ormige Dose zu konstruieren. Die Dose soll zus"atzlich die Bedingung erf"ullen, ein 
Fassungsverm"ogen (Volumen) von einem Liter zu haben. Sind $r$ der Radius und $h$ die H"ohe der Dose, so soll also $V = \pi r^2 \cdot h = 1$ sein
(auf die physikalischen Einheiten wurde der mathematischen Einfachheit halber verzichtet -- in der Praxis ist es allerdings unumgänglich, mit den
Einheiten zu rechnen).  Um Arbeitsmaterial zu sparen, suchen wir nach derjenigen Dose, die eine m"oglichst kleine Oberfl"ache
$O = 2 \cdot \pi r^2 + 2 \pi r h$ hat. Hier ist die Oberfläche $O$ eine Funktion in Abhängigkeit von Radius $r$ und Höhe $h$ der Dose.

Mathematisch formuliert f"uhrt die Aufgabe auf die Suche nach einem Minimum f"ur die Funktion $O$ der Oberfl"ache, wobei für die Berechnung des Minimums nur 
Werte f"ur $r$ und $h$ zugelassen werden, f"ur die auch die Bedingung "uber das Volumen $V = \pi r^2 \cdot h = 1$ erf"ullt ist. Eine solche zus"atzliche
Bedingung bei der Suche nach Extremstellen wird auch \textbf{Nebenbedingung} genannt.

\begin{MXInfo}{Optimierungsaufgabe}
In einer \MEntry{Optimierungsaufgabe}{Optimierungsaufgabe} wird eine
Extremstelle $x_{\text{ext}}$ einer Funktion $f$ gesucht, die eine 
gegebene Gleichung $g(x_{\text{ext}}) = b$ erf"ullt.

Wenn ein globales Minimum gesucht wird, spricht man auch von einer 
\MEntry{Minimierungsaufgabe}{Minimierungsaufgabe}. Wenn ein Maximum gesucht
wird, hei"st die Optimierungsaufgabe eine 
\MEntry{Maximierungsaufgabe}{Maximierungsaufgabe}.

Die Funktion $f$ hei"st \MEntry{Zielfunktion}{Zielfunktion}, und die 
Gleichung $g(x) = b$ wird \MEntry{Nebenbedingung}{Nebenbedingung} der 
Optimierungsaufgabe genannt.
\end{MXInfo}


\end{MXContent}

\begin{MXContent}{Beispiel}{Beispiel}{STD}

Sehen wir uns das Beispiel aus der Einführung dieses Abschnitts etwas genauer an und minimieren die Oberfläche einer zylinderförmigen Dose bei einem vorgegebenen Volumen (Grundfläche mal Höhe)
%
\begin{eqnarray*}
V = \pi r^2 h = 1,
\end{eqnarray*}
%
wobei $r$ der Radius der Grundfläche und $h$ die Höhe der Dose sind. Die Oberfläche wird zusammengesetzt aus zweimal dem Deckel (je $\pi r^2$) und der Mantelfläche ($\pi r h$) und wir erhalten $O = 2 \pi r^2 + 2 \pi r h$. Die Oberfläche der Dose ist eine Funktion von Radius $r$ und Höhe $h$. Im Gegensatz dazu wird dem Volumen ein fester Wert zugeordnet (Nebenbedingung). Wir können also schreiben:
%
\begin{eqnarray*}
O\left(r, h\right) = 2 \pi r^2 + 2 \pi r h.
\end{eqnarray*}
%
Wegen der Nebenbedingung, dass $V = \pi r^2 h = 1$ sein soll, kann man dieses Problem in ursprünglich zwei Variablen ($r$ und $h$) auf ein Problem mit nur noch einer Variablen reduzieren. Formen wir das Volumen nach der Höhe der Dose um:
%
\begin{eqnarray*}
\pi r^2 h = 1\\
h = \frac{1}{\pi r^2}
\end{eqnarray*}
%
Setzen wir diese Formel in die Fuktion $O(r,h)$ ein, reduziert $O$ sich auf eine Funktion in nur einer Variablen:
%
\begin{eqnarray*}
O\left(r, h\right) = 2 \pi r^2 + 2 \pi r h = 2 \pi r^2 + 2 \pi r \frac{1}{\pi r^2} = 2 \left(\pi r^2 + \frac{1}{r}\right) = O\left(r\right)
\end{eqnarray*}
%
Nach dieser Umformung können wir die Frage nach der minimalen Oberfläche der Dose ganz analog zu den Extremwerten von Funktionen bearbeiten. Bilden wir also die erste Ableitung der Funktion $O$ nach der Variablen $r$ und setzen diese gleich Null:
%
\begin{eqnarray*}
O'\left(r\right) = 2 \left(2\pi r - \frac{1}{r^2}\right) &=& 0\\
\Leftrightarrow\quad 2\pi r &=& \frac{1}{r^2}\\
\Leftrightarrow\quad 2\pi r^3 &=& 1\\
\Leftrightarrow\quad r^3 &=& \frac{1}{2\pi}\\
\Leftrightarrow\quad r &=& \sqrt[3]{\frac{1}{2\pi}}
\end{eqnarray*}
%
Setzen wir dieses Ergebnis in die zweite Ableitung von $O$ ein, um zu überprüfen, ob wir wirklich ein Minimum gefunden haben:
%
\begin{eqnarray*}
O''\left(\sqrt[3]{\frac{1}{2\pi}}\right) = 4\pi + \frac{4}{\left(\sqrt[3]{\frac{1}{2\pi}}\right)^3} = 12\pi > 0
\end{eqnarray*}
%
Für den Radius $r = \sqrt[3]{\frac{1}{2\pi}}$ wird die Oberfläche der zylindrischen Dose mit dem gegebenen Volumen $V = 1$ minimal. Die Höhe erhalten wir in diesem Fall zu $h = \frac{1}{\pi \left(\sqrt[3]{\frac{1}{2\pi}}\right)^2} = \sqrt[3]{\frac{4}{\pi}}$. Stellen wir eine Dose mit dieser Bemaßung her, werden wir für ein vorgegebenes Volumen auch den Materialverbrauch minimieren.

%Wir bestimmen den Radius $r$ des Grundkreises und die H\"ohe $h$ eines Kreiszylinders maximalen Volumens unter der Nebenbedingung,
%dass dieser Kreiszylinder in eine Kugel vom Radius~$R \in \R^+$ passen soll.
%\ \\ \ \\
%Das Volumen des Kreiszylinders betr"agt $V = \pi r^2 h$. Wenn der 
%Kreiszylinder einen m"oglichst gro"sen Teil der Kugel ausf"ullt, ber"uhren 
%Boden und Deckel die Kugel.
%\ \\
%\MUGraphics{\MPfadBilder/BildGrundkreis.png}{width=0.5\linewidth}{Skizze: Schnitt durch eine Mittelebene der Kugel und des Zylinders}{width:400px}
%\ \\
%Die Abbildung oben zeigt einen Schnitt durch die Kugel (grün) und den Zylinder (schwarz gepunktet), sodass eine vollständige Seitenansicht des
%Problems ermöglicht wird. Der Zylinder steht in dieser Skizze nicht auf seiner Grundfläche, sondern er liegt auf der Seite. Wir erkennen hier, dass die halbe Höhe des Zylinders $h/2$, der %Zylinderradius $r$ und der Kugelradius $R$ ein
%rechtwinkliges Dreieck bilden. Wenden wir hier den Satz von Pythagoras an, finden wir die Beziehung $R^2 = r^2 + \left(\frac{1}{2} h\right)^2$, die uns
%die \textbf{Nebenbedingung} beschreibt, dass der Zylinder die Kugeloberfläche berühren soll und die beiden Bemaßungen $r$ und $h$ in eine Abhängigkeit
%voneinander bringt.
%\ \\
%Wollen wir diese Beziehung in die Formel für das Zylindervolumen $V = \pi r^2 h$ einsetzen, müssen wir sie zunächst nach $r^2$ auflösen:
%$$
%r^2 = R^2 - \left(\frac{1}{2} h\right)^2
%$$
%Setzen wir diesen Ausdruck in das Zylindervolumen ein, dann erhalten wir eine Formel für das Volumen als Funktion der Zylinderhöhe $h$:
%$$
%V(h) \;=\; \pi \left( R^2 - \frac{1}{4} h^2 \right) h \;  = \;\pi R^2 h - \frac{\pi}{4} h^3
%$$
%f"ur $0 \leq h \leq R$, wobei alle anderen Größen in dieser Funktion bekannt sind. 
%Untersuchen wir die beiden Grenzen des Definitionsbereichs von $h$, also $h = 0$ und $h = R$, dann erhalten wir für das Volumen jeweils $V(h=0) = 0$ und $V(h=R) = 0$. Daher muss die gesuchte %Extremstelle, an der $V$ maximal ist, in $]0, R[$ liegen. Da die Funktion $V: [0, R] \rightarrow \R$ differenzierbar ist, ist daf"ur
%$V'(h) = 0$ eine notwendige Bedingung. Leiten wir das Volumen nach $h$ ab, $V'(h) = \pi R^2 - \frac{3 \pi}{4} h^2$, folgt hieraus
%$$
%h^2\; =\; \frac{4}{3} R^2\quad\text{bzw.}\quad h = 2 R \sqrt{\frac{1}{3}} = \frac{2}{\sqrt{3}} R.
%$$
%Die negative Lösung dieser Gleichung können wir wegfallen lassen, da $h \geq 0$ gilt. 
%Da $h = \frac{2}{\sqrt{3}} R$ die einzige Stelle eines Extremwertes im zugelassenen Bereich für $h$ ist
%und $V(h) \geq 0$ gilt, handelt es sich um die gesuchte Maximalstelle.
%Somit ist
%\begin{eqnarray*}
%V_{\mathrm{max}} & = & V\left(2 R \sqrt{\frac{1}{3}}\right) \\
% & = & \pi \left( R^2 - \frac{1}{4} \cdot 4 R^2 \frac{1}{3} \right) %
%  \cdot 2 R \sqrt{\frac{1}{3}} \\
% & = & \frac{4}{3} \pi R^3 \cdot \sqrt{\frac{1}{3}} \\
% & = & \frac{4}{3\sqrt{3}}\pi R^3 %%
%\end{eqnarray*}
%das maximale Volumen.
%
%Unsere ursprüngliche Aufgabe bestand darin, den Radius des Kreiszylinders zu bestimmen. Dafür können wir die Gleichung
%$$
%r^2 = R^2 - \left(\frac{1}{2} h\right)^2
%$$
%verwenden. Setzen wir hier die Höhe $h = \frac{2}{\sqrt{3}} R$ ein, dann erhalten wir den gesuchten Radius
%$$
%r = R \sqrt{\frac{2}{3}},
%$$
%der die Forderungen aus der anfänglichen Aufgabenstellung erfüllt.

\end{MXContent}

%end of file.

